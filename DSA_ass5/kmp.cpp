#include<iostream>
#include<string.h>
#define lint long long int 
using namespace std;

void compute(string pat)
{
    int l = 0;
    int len = pat.size();
    for (int i =0;i < len;i++)
    {
        ;
    }
}
int main()
{
    string text;
    string pat;
    cin>>text;
    cin>>pat;
}